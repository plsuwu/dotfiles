> [old arch config](https://github.com/plsuwu/dotfiles/tree/main)
>
> still using the above while i build the rest of this config + generally get my head around nix
>
> the learning curve is something else but nixos is actually sick as hell....
